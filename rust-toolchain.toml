[toolchain]
# Exact toolchain observed by the exact-head court on 2026-08-12.
# Reproducibility is part of receipt identity; do not float this silently.
channel = "nightly-2026-08-12"
components = ["rustfmt", "clippy"]
profile = "minimal"
